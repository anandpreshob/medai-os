# MedAI Cloud Orchestrator Requirements (CPU-only)
#
# Serves the MONAI Label REST surface the Agent/orchestrator needs
# (/datastore, /batch, /dicomseg) and dispatches inference to a managed cloud
# backend (Vertex AI). Deliberately EXCLUDES the heavy GPU model stacks that the
# inference image carries: SAM2/SAM3, detectron2, nnU-Net, TotalSegmentator,
# BiomedParse deps, and all baked model weights.
#
# torch/torchvision are installed as CPU wheels in Dockerfile.orchestrator.

# Core framework (same monai pin/extras as inference for import compatibility)
monai[nibabel, skimage, pillow, tensorboard, gdown, ignite, torchvision, itk, tqdm, lmdb, psutil, openslide, fire, mlflow]==1.5.1
numpy==1.26.4

# Web server (pinned)
uvicorn==0.29.0
pydantic==2.7.0
pydantic-settings==2.2.1
python-dotenv==1.0.1
fastapi==0.110.2
python-multipart==0.0.18
httpx==0.27.0

# Scheduling and caching (pinned)
schedule==1.2.1
timeloop==1.0.2
expiringdict==1.2.2
expiring-dict==1.1.0
cachetools==5.3.3
watchdog==4.0.0

# DICOM handling (pinned - critical for datastore + DICOM-SEG write-back)
dicomweb-client[gcp]==0.59.1
pydicom==2.4.4
pydicom-seg==0.4.1
pynetdicom==2.0.2

# Medical imaging formats (pinned)
pynrrd==1.1.1
dicom2nifti==2.4.11
nibabel==5.3.3
SimpleITK==2.5.3

# Utilities (pinned)
girder-client==3.2.3
einops==0.7.0
pyyaml==6.0.1
filelock==3.11.0
shapely==2.0.4
requests==2.28.2
requests-toolbelt==1.0.0
urllib3==1.26.20
scikit-learn==1.7.2
scipy==1.15.3
Pillow==12.1.0
opencv-python-headless==4.7.0.72

# Auth (pinned)
passlib==1.7.4
pyjwt==2.8.0
bcrypt==4.1.2
google-auth==2.29.0
huggingface_hub==0.36.0

# Cloud batch backend SDKs (Vertex AI)
google-cloud-aiplatform==1.60.0
google-cloud-storage==2.16.0
